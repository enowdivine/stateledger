// @stateledger/prisma — Prisma adapter for stateledger
//
// `createPrismaAdapter` wraps a PrismaClient (or a TransactionClient) so
// stateledger can read and write the transitions table. Empty for now;
// implementation lands once @stateledger/core's Adapter interface is locked.

export const version = "0.0.0";
