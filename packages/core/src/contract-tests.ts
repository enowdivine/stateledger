// @stateledger/core/contract-tests
//
// Generic test suite that adapter authors run against their adapter to
// verify it honors the Adapter contract — locking semantics, history
// ordering, error wrapping, transaction rollback behavior. Empty for now;
// fills in once the Adapter interface is locked.

export const placeholder = true;
